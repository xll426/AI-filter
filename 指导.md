
将这个项目目前训练的情况作为baseline上传github
ignore  /mnt/d/fudan/prefilter_clean/data这里面的数据
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/xll426/AI-filter.git
git push -u origin main



https://github.com/karpathy/autoresearch 这个项目对于模型训练的优化可以参考几个思路
1.data数据读入不变 模型结构不变（目前是重参数化训练 可以搞成更多的卷积 但是最后export出的onnx必须跟现在的一致 3*3卷积） 
2.模型优化器 loss可以进行优化 让我的模型结构去逼近ref出来的结果 传统算法就是为了滤波的 不重要的地方给他去噪 平滑 主要的边界保留 尽量不要给我滤波/mnt/d/fudan/prefilter_clean/ref.py
3.目前的训练是基于/mnt/d/fudan/prefilter_clean/weights （这个模型是在未知的一个数据集里训练过 基于这一套现在的模型 也已经有个较好的结果）去微调一下我现在的/mnt/d/fudan/prefilter_clean/data 让他在test里面有一个更好的泛化性 微调的epoch iter根据我现在的数据集大小规模合理制定
4.终极目的是我的输入是8bit的int8 像素顶多是0-256 卷积权重也是int 但是多少比特依照情况来定 8——17bit之间 输出也是int8 因为我需要训练一个这样的模型去硬件化部署 去帮我调研一下 可不可行 先去初始值 这个weigt设置多少bit的整数 然后去去训练怎么梯度回传 我需要你不断优化 基于上传的git 去尝试 尝试有问题或者方式不对需要回退 并且在git进行记录 autoresearch 上面说模型训练一轮或者就能看出来什么情况 去判断到底继续还是说要提前中断 去评估模型好坏的指标统算法就是为了滤波的 不重要的地方给他去噪 平滑 主要的边界保留 可视化看主观是一方面 另外就是psnr ssim这种测试推理用的指标 
